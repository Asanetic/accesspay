/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/ap',      // dairy sacco graders portal tells Next.js this app lives under /dairy
    assetPrefix: '/ap',   // optional: ensures static assets load correctly
    reactStrictMode: true
  };
  
  module.exports = nextConfig;
  
  